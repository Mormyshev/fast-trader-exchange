meta:
  environment: nodejs
  toolchain:
    name: npm
    version: "20"
build:
  # Используем стандартное поле command вместо additionalCommands
  command: npm install --include=dev && npm run build
run:
  command: npm run start
  persistenceMount: /data
  containerPort: "3000"
