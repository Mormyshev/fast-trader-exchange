meta:
  environment: nodejs
  toolchain:
    name: npm
    version: "20"
build:
  skip: false
  # Внедряем строку подключения к БД прямо в процесс сборки Amvera
  additionalCommands: npm install --include=dev && NEXT_FONT_GOOGLE_MOCKED=true DATABASE_URL="postgresql://adminftt:Karakum8125@amvera-mormyshevoleg-cnpg-fast-trader-db-rw:5432/fastdb" npm run build
run:
  scriptName: null
  scriptArguments: null
  nodeArguments: null
  command: npm run start
  persistenceMount: /data
  containerPort: "3000"
